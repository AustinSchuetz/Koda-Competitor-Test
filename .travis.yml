language: node_js
node_js:
  - "8"
install:
  - npm install
  - npm install -g firebase-tools
   firebase deploy — project koda-competitor-testing— token $FIREBASE_TOKEN.
  firebase deploy
script:
  - echo "Deploy!!"
after_success:
  - echo "After Success!"
