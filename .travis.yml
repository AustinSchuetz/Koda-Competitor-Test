language: node_js
node_js:
  - "8"
install:
  - npm install
  - npm install -g firebase-tools
script:
  - echo "Deploy!!"
  - npm test
after_success:
   - firebase deploy — project koda-competitor-testing— token $FIREBASE_TOKEN.
