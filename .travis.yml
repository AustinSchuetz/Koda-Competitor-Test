language: node_js
node_js:
  - "8"
install:
  - npm install -g firebase-tools
script:
  - echo "Deploy!!"
after_success:
  - echo "After Success Message!"
   - firebase deploy — project koda-competitor-testing— token $FIREBASE_TOKEN.
