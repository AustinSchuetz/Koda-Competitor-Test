language: node_js
node_js:
 — “7”
script:
 — echo “Deploy!!”
-npm install
install:
 — npm install -g firebase-tools
after_success:
 — firebase deploy — project koda-competitor-testing — token $FIREBASE_TOKEN.