language: node_js
node_js:
  - "8.12.0"
install:
  - npm install
  - npm install -g firebase-tools
script:
  - echo "Deploy!!"
after_success:
   firebase deploy — project koda-competitor-testing— token $FIREBASE_TOKEN
