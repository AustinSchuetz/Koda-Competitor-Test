language: node_js
node_js:
  - "8.12.0"
script:
  - echo "Deploy!"
install:
  - npm install -g firebase-tools
  - npm get prefix
after_success:
   - firebase deploy -  project koda-competitor-testing - token $FIREBASE_TOKEN.
